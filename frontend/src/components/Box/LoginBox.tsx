import styled from "styled-components";
import theme from "../theme";
import { ColumnDiv } from "./Base";

export const LoginBox = styled(ColumnDiv)`
    width: 300px;
    height: 500px;

    background-color: ${theme.boxcolor.background};
`