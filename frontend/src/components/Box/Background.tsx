import styled from "styled-components";
import theme from "../theme";
import { CenterDiv } from './Base'

export const LoginBackground = styled(CenterDiv)`
    width: 100%;
    height: 100%;

    background-color: ${theme.boxcolor.background}
`